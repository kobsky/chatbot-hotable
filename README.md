# Restaurant Chatbot MVP

## Instalacja

`pip install -r requirements.txt`

## Konfiguracja

Zmień nazwę pliku `.env.example` na `.env` i uzupełnij brakujące klucze.

## Uruchomienie

`python app.py`
